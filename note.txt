import React from 'react';

const Line = ({ start, end, strokeColor, strokeWidth }) => {
  const getPath = () => `M ${start.x} ${start.y} L ${end.x} ${end.y}`;

  return (
    <svg width="100%" height="100%">
      <line
        x1={start.x}
        y1={start.y}
        x2={end.x}
        y2={end.y}
        stroke={strokeColor}
        strokeWidth={strokeWidth}
      />
    </svg>
  );
};

export default Line;